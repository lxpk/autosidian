# Autosidian E2E

This vault is for **local integration tests** only. The plugin is copied here by `npm run test:integration` (see the repo file `e2e/README.md`).

Install the four partner plugins from the community list when you need full end-to-end behavior; the E2E UI test expects the **“Required plugins not enabled”** notice when they are missing.
